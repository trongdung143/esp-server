services:
  - type: web
    name: esp32_server
    env: python
    plan: free
    pythonVersion: 3.12
    buildCommand: 
      pip install -r requirements.txt
    startCommand: "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
